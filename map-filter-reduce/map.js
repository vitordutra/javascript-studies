const numbers = [1, 2, 3];

// Map -> retorna o mesmo número de itens do array original só que transformado.
// Sempre tem que retornar um valor
const doubleNumbers = numbers.map((item) => item * 2);

console.log(doubleNumbers);
